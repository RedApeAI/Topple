fastapi>=0.110
uvicorn>=0.29
pydantic>=2.6
pydantic-settings>=2.2
motor>=3.4
qdrant-client[fastembed]>=1.9
openai>=1.30
pyyaml>=6.0
httpx>=0.27
redis>=5.0
# Both agent planes are LangGraph state graphs. Pinned below 2 because the
# graph API changes shape between majors.
langgraph>=1.2,<2
# Runtime, not tests: app/mcp/client.py connects to the BFF's MCP server to
# discover each user's connector tools before every Operator prompt.
mcp>=2.0
