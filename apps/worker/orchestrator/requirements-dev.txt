# Test-only dependencies. Kept out of requirements.txt so the production image
# does not ship a test runner or an in-memory Mongo fake.
-r requirements.txt

pytest>=8.0
pytest-asyncio>=0.23
mongomock-motor>=0.0.30
