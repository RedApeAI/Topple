[project]
name = "plucia-call"
version = "0.0.0"
description = "Python call API service for Plucia."
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn[standard]>=0.30.0",
]
