[project]
name = "redape-heartbeat"
version = "0.0.0"
description = "Heartbeat service for RedApeAI."
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn[standard]>=0.30.0",
]
